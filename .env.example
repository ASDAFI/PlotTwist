# Absolute host paths are recommended. Each input stays read-only.
DATASET_DIR=./dataset-studio/examples/dataset
RESPONSES_DIR=./dataset-studio/examples/responses
EXPORT_DIR=./dataset-studio/exports
# On Linux, set these to the output of id -u and id -g.
PLOTTWIST_UID=1000
PLOTTWIST_GID=1000
