# Dataset Studio

This directory contains the current PlotTwist Dataset Studio implementation:
the React editor, FastAPI persistence service, examples, documentation,
fixtures, and browser preview.

Use the repository-root [README](../README.md) for installation and Docker
commands. The root package and Compose files are workspace entrypoints; this
directory owns the Dataset Studio product code and data fixtures.
